export const ADMIN_SLUG = 'admin';

/** Module folder names under src/modules excluded from route/slug discovery (e.g. public auth). */
export const PERMISSION_ROUTE_SKIP_MODULES = ['auth'] as const;
